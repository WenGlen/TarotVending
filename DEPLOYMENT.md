# 部署指南 - GitHub + Zeabur

本指南將幫助您將 Tarot Vending Machine 應用部署到 GitHub 並使用 Zeabur 進行 API 串接。

## 📋 前置準備

1. **GitHub 帳號**
2. **Zeabur 帳號**（可免費註冊：https://zeabur.com）
3. **Gemini API Key**（從 Google AI Studio 取得：https://makersuite.google.com/app/apikey）

## 🚀 部署步驟

### 步驟 1: 上傳到 GitHub

1. **初始化 Git 倉庫**（如果還沒有）
   ```bash
   cd TarotVending
   git init
   ```

2. **添加所有文件**
   ```bash
   git add .
   git commit -m "Initial commit: Tarot Vending Machine"
   ```

3. **在 GitHub 創建新倉庫**
   - 前往 https://github.com/new
   - 創建一個新的倉庫（例如：`tarot-vending-machine`）
   - **不要**初始化 README、.gitignore 或 license

4. **連接並推送**
   ```bash
   git remote add origin https://github.com/YOUR_USERNAME/tarot-vending-machine.git
   git branch -M main
   git push -u origin main
   ```

### 步驟 2: 安裝依賴

在推送前，確保已安裝所有依賴：

```bash
npm install
```

這會安裝：
- `express` - 後端 API 服務器
- `cors` - 跨域請求處理
- 其他前端依賴

### 步驟 3: 在 Zeabur 部署

1. **登入 Zeabur**
   - 前往 https://zeabur.com
   - 使用 GitHub 帳號登入（推薦）

2. **創建新專案**
   - 點擊「New Project」
   - 選擇「Import from GitHub」
   - 選擇您的 `tarot-vending-machine` 倉庫

3. **配置服務**
   - Zeabur 會自動偵測這是一個 Node.js 專案
   - 確認以下設定：
     - **Build Command**: `npm run build`
     - **Start Command**: `npm start`
     - **Output Directory**: `dist`

4. **設定環境變數**
   - 在 Zeabur 專案頁面，點擊「Environment Variables」
   - 添加以下環境變數：
     ```
     GEMINI_API_KEY=你的_Gemini_API_Key
     NODE_ENV=production
     PORT=3000
     ```
   - **重要**：`GEMINI_API_KEY` 必須設定，否則 API 無法運作

5. **部署**
   - 點擊「Deploy」開始部署
   - Zeabur 會自動：
     - 安裝依賴
     - 執行 `npm run build` 構建前端
     - 啟動 `npm start` 運行後端服務

### 步驟 4: 驗證部署

1. **檢查部署狀態**
   - 在 Zeabur 專案頁面查看部署日誌
   - 確認沒有錯誤訊息

2. **測試 API**
   - Zeabur 會提供一個 URL（例如：`https://your-project.zeabur.app`）
   - 訪問 `/api/health` 端點檢查後端是否正常：
     ```
     https://your-project.zeabur.app/api/health
     ```
   - 應該返回：`{"status":"ok","timestamp":"..."}`

3. **測試前端**
   - 訪問主頁面，測試塔羅牌功能
   - 確認 API 請求正常運作

## 🔧 本地開發

### 開發模式

1. **啟動後端服務**（終端 1）
   ```bash
   npm run dev:server
   ```

2. **啟動前端開發服務器**（終端 2）
   ```bash
   npm run dev
   ```

3. **訪問應用**
   - 前端：http://localhost:5173
   - 後端 API：http://localhost:3000/api/health

### 本地環境變數

創建 `.env` 文件（不要提交到 Git）：
```
GEMINI_API_KEY=your_local_api_key
NODE_ENV=development
PORT=3000
```

## 📁 專案結構

```
TarotVending/
├── server.js              # 後端 API 服務器
├── package.json           # 專案依賴和腳本
├── vite.config.js         # Vite 配置（包含開發代理）
├── zeabur.json           # Zeabur 部署配置
├── src/
│   ├── api/
│   │   └── gemini.js     # Gemini API 客戶端（已更新為使用後端）
│   └── ...
└── dist/                 # 構建輸出（由 npm run build 生成）
```

## 🔐 安全注意事項

1. **API Key 安全**
   - ✅ **正確**：API key 存儲在 Zeabur 環境變數中
   - ❌ **錯誤**：不要將 API key 提交到 GitHub
   - ✅ `.gitignore` 已包含 `.env` 文件

2. **CORS 配置**
   - 後端已配置 CORS，允許跨域請求
   - 生產環境中，可以限制特定域名

## 🐛 常見問題

### 問題 1: API 返回 500 錯誤
**解決方案**：檢查 Zeabur 環境變數中是否正確設定了 `GEMINI_API_KEY`

### 問題 2: 前端無法連接到後端
**解決方案**：
- 確認 Zeabur 部署成功
- 檢查瀏覽器控制台的錯誤訊息
- 確認 API 端點路徑正確（`/api/gemini-chat`）

### 問題 3: 構建失敗
**解決方案**：
- 檢查 `package.json` 中的依賴是否正確
- 確認 Node.js 版本（建議 18+）
- 查看 Zeabur 構建日誌

### 問題 4: 本地開發時 API 無法連接
**解決方案**：
- 確認後端服務正在運行（`npm run dev:server`）
- 檢查 `vite.config.js` 中的 proxy 配置
- 確認端口 3000 沒有被其他程序佔用

## 📝 更新部署

當您更新代碼後：

1. **提交更改到 GitHub**
   ```bash
   git add .
   git commit -m "更新說明"
   git push
   ```

2. **Zeabur 自動部署**
   - Zeabur 會自動偵測 GitHub 的更改
   - 觸發新的部署流程
   - 通常幾分鐘內完成

## 🎉 完成！

您的塔羅牌販賣機應用現在已經部署到 Zeabur 了！

如有任何問題，請檢查：
- Zeabur 部署日誌
- 瀏覽器控制台錯誤
- 後端 API 健康檢查端點

